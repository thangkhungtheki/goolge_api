<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video.js Segment Handling</title>

    <!-- Thêm thư viện Video.js -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/video.js/8.6.0/video-js.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/video.js/8.6.0/video.min.js"></script>
</head>

<body>
    <!-- Phần tử để chứa video player -->
    <video id="my-video" class="video-js" controls preload="auto" width="640" height="360">
        <source src="png/output.m3u8" type="application/x-mpegURL">
    </video>

    <script>
        // Khởi tạo Video.js player
        var player = videojs('my-video');

        // Đăng ký sự kiện khi phân đoạn được tải xuống
        player.on('segmentLoaded', function (event, segment) {
            // Cắt phân đoạn từ byte 112 trở đi
            //var cutSegmentData = segment.data.slice(11200);

            // Tạo một Blob mới từ dữ liệu đã cắt
            //var cutSegmentBlob = new Blob([cutSegmentData]);
            //console.log(cutSegmentData)
            // Chuyển đổi Blob thành URL để phát
            //var cutSegmentURL = URL.createObjectURL(cutSegmentBlob);

            // Thay đổi nguồn của video player để phát phân đoạn đã cắt
            //player.src({ type: 'application/x-mpegURL', src: cutSegmentURL });

            // Bắt đầu phát lại video từ đoạn đã cắt
            //player.play();
        });
    </script>
</body>

</html>